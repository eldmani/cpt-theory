# Information Theory — Configuration Propagation Theory

## The f(x) = x² Insight

Consider a generative rule `f(x) = x²` applied to seed `x₀ = 2`:

```
Step 0: x₀ = 2         → 1 bit to describe
Step 1: f(x₀) = 4      → 2 bits
Step 2: f²(x₀) = 16    → 4 bits
Step 3: f³(x₀) = 256   → 8 bits
Step n: x₀^(2^n)       → 2ⁿ bits
```

Information to describe the state grows **exponentially**. The seed and rules are unchanged. This is not violation — this is the definition of a generative rule.

**The seed contains a compressed program. The universe is the running of that program. Each propagation step makes implicit information explicit.**

---

## Three Distinct Quantities — All Simultaneously True

| Quantity | Direction | Measures |
|---|---|---|
| Information per node `i(t)` | ↓ Decreasing | Dilution — each node carries less of the original seed |
| Total structural information `I(t)` | ↑ Increasing | New configurations generated by rule application |
| Kolmogorov complexity of rules | → Constant | The seed + c() + t() — the program itself never changes |

These are not contradictions. They operate at different levels.

### The Exact Relationship

```
I(t) = I₀ · a(t)/a(0)    [information grows as scale factor]
```

The information amplification rate equals the Hubble parameter:

```
λ(t) = ȧ/a = H(t)
```

**Physical expansion = Information expansion.** They are the same phenomenon.

---

## The Universe as Compressed Program

```
Seed (t=0):   Small compressed program, contains c(), t(), initial state
              Kolmogorov complexity K(universe_at_t) ≈ K(seed) + K(t) = constant

t = now:      Vast explicit description
              Explicit information: I₀ × a(now)/a(0) = I₀ × 10⁶¹ (enormous)

Kolmogorov:   Still just: seed + step counter
```

**The universe looks increasingly complex as it evolves. But it has always been fully described by the seed.** We are watching the decompression of a small, ancient compressed file.

Heat death is not the end — it is when the universe is **most expanded** and thus most explicit. The rules are still running. The generation of new information continues.

---

## The Soft Boundary `q` and Information Leakage

The Hubble horizon, black hole horizons, and the Planck scale are all instances of a **soft boundary** `q`:

```
Configuration space:  |--playing field--|--q--|--beyond q--|
                           < q        boundary    > q
```

At the boundary, c() has small but **non-zero** amplitude for paths crossing into `> q`:

```
c(v_near_q) = Σ_{v'<q} α_{vv'}|v'⟩  +  ε·Σ_{v'>q} β_{vv'}|v'⟩
```

where `ε ≪ 1` but `ε ≠ 0`.

### This IS Quantum Tunneling

The exponential decay of c() amplitude beyond `q` is exactly the WKB tunneling formula:

```
P(tunnel) ∝ e^{-2∫κ(x)dx}
```

Quantum tunneling, radioactive decay, Hawking radiation, and cosmic horizon leakage are **all the same mechanism** — c() leakage through a soft boundary — at different scales.

| Scale | Playing field | Boundary q | Leakage | Effect |
|---|---|---|---|---|
| Atomic | Classical region | Potential barrier | Quantum tunneling | Particle escapes |
| Nuclear | Nucleus | Coulomb barrier | Alpha decay | New nucleus |
| Black hole | Interior | Event horizon | Hawking radiation | Information recovered |
| Cosmic | Observable universe | Hubble horizon | Dark energy inflow | Universe accelerates |

---

## Information Conservation — Globally Maintained

| Scope | Information | Status |
|---|---|---|
| Our playing field | Decreases (leaks out) | Open system |
| Beyond q | Increases (accumulates) | Accumulates |
| **Full configuration space** | **Constant** | **t() bijective globally** |

The "loss" we perceive is an artifact of treating our region as closed. It isn't.

```
I_total = I_{<q}(t) + I_{accumulated,>q}(t) = constant
```

---

## Seeding New Universes

When accumulated leaked information reaches critical threshold:

```
∫_{t₀}^{t_seed} J_leak dt ≥ I_critical
```

A new fixed-point condition is satisfied: `v₀_new = T̂·Ĉ(v₀_new)`

**A new resolution front begins propagating → new Big Bang → new universe.**

`I_critical` = minimum information required for the fixed-point equation to have a solution. This is not an assertion — it is a mathematical property of the fixed-point equation itself.

### What the Child Universe Inherits

The child universe's seed = the accumulated leaked configuration from the parent. Therefore:
- The c()/t() encoding of the child is **derived from the parent's** encoding (not identical — leakage is transformative)
- Child physics ≠ parent physics, but related
- Different leakage paths → different encodings → different physical constants
- This provides a mechanism for cosmological natural selection (Smolin): universes that produce many black holes have more descendants

---

## Heat Death Reframed

**Previous interpretation:** Heat death = maximum entropy = dead end.

**CPT interpretation:** Heat death = maximum dilution = maximum leakage rate = **peak seeding productivity**.

```
Early universe:  high density, low leakage (far inside q boundary)
Middle universe: increasing dilution, increasing leakage
Heat death:      maximum dilution → maximum c() amplitude at q
                 → maximum rate of new universe seeding
```

**The universe at heat death is not dead. It is maximally generative.**

Heat death is not the end of the universe — it is the moment the universe contributes most to creating descendants.

---

## The Fractal Tree of Universes

```
PARENT UNIVERSE
    │
    ├─ Dark energy = inflow from parent's diluted configuration
    │
OUR PLAYING FIELD
    │
    ├─ Black hole A → q boundary → seed → CHILD UNIVERSE A
    │         └─ Black hole A1 → seed → GRANDCHILD A1
    │
    ├─ Black hole B → seed → CHILD UNIVERSE B
    │
    └─ Hubble horizon leakage → CHILD UNIVERSE C (slow seeding)
```

This is not sequential (one universe after another) — it is a **branching, simultaneous tree**. Parent universes continue existing while children form.

**The universe is a self-reproducing configuration that has been doing this eternally and will continue doing so.**

---

## Black Holes as Universe Seeds — With Mechanism

- A black hole compresses configurations toward maximum information density
- The event horizon is a **local q boundary** within our playing field
- Information squeezed toward horizon → c() amplitude for tunneling increases dramatically
- Beyond the horizon, accumulated information reaches seed threshold
- **The singularity is not a breakdown — it is a threshold crossing**
- Beyond it: a new resolution front → new universe

Every black hole is potentially a universe. Our universe may sit inside a black hole in a parent universe. This is consistent with the theory's structure (the external environment is the region > q, which we can never observe directly).
